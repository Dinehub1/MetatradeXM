#!/usr/bin/env python3
"""
Legacy placeholder.

The system is Supabase-only now, so the old SQLite migration flow has been retired.
"""


def main():
    print("Supabase is already the only database source. No SQLite migration is required.")


if __name__ == "__main__":
    main()
